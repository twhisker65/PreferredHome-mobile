BUILD 3.1.10.1 HOTFIX
======================

Issue
- Metro bundling failed because app/eslint.config.js was being bundled into the React Native runtime and imported Node built-ins (path) via eslint-config-expo/eslint-plugin-import.

Fix (minimal)
- Replace app/eslint.config.js with a no-op export that does NOT import any Node modules.
- Root-level eslint.config.js remains the canonical ESLint config (unchanged).

Files included
- app/eslint.config.js
- BUILD_3.1.10.1_README.txt
