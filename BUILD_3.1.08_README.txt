BUILD 3.1.08
Title: UI corrections (Home stats, Listings card, Add toggles/time, Calendar revert)

What changed
- Home: Stats always show Listings count; min/max/avg derived from base rent when available.
- Listings: Card photo enlarged 1.25x (64->80). Status pill stretches to photo width.
- Add: Toggle visuals corrected (white track + blue dot on YES). Numeric "Next" improves. Auto-scroll to focused field while keyboard open. Viewing Time now uses a picker modal (no manual typing).
- Compare: Card/Table view icons updated (grid + list).
- Calendar: Removed title/subtitle. Calendar is horizontal month paging (no vertical month scrolling). Appointments list remains under calendar and includes contact name.

Files included in this ZIP are ONLY the changed files for this build.
