BUILD 3.1.13 — Field Name Cleanup
Generated: March 5, 2026

SUMMARY
-------
Cleaned up all field name lookups across the codebase to use
single camelCase names only. Removed all multi-alias fallback
arrays and pick() helper functions where used for field lookup.
The app now reads field names exactly as the Render API delivers them.

FILES CHANGED
-------------
lib/listingsNormalize.ts
  - Removed pick() helper function entirely
  - All field reads now use direct camelCase: raw.fieldName
  - Removed all snake_case and Title Case fallback aliases
  - Affects: buildingName, streetAddress, zipCode, city, state,
    unitNumber, unitType, bedrooms, bathrooms, squareFootage,
    baseRent, parkingFee, amenityFee, adminFee, utilityFee,
    otherFee, status, preferred, photoUrl, id, listingSite

app/(tabs)/calendar.tsx
  - Removed pick() helper function
  - Removed multi-alias fallback arrays for viewingAppointment
    and contactName
  - Both fields now read directly: raw.viewingAppointment,
    raw.contactName
  - Simplified parseDateTime() — removed unused rawTime parameter

FILES NOT CHANGED (no field name issues found)
-----------------------------------------------
app/(tabs)/index.tsx        — reads from ListingUI type only, no raw field access
app/(tabs)/listings.tsx     — reads from ListingUI type only, no raw field access
app/(tabs)/add.tsx          — form only, no field reads from API
lib/types.ts                — type definitions only, no field lookups
lib/api.ts                  — HTTP layer only, no field names
lib/useListings.ts          — calls normalizeListing(), no direct field access
components/ListingCard.tsx  — reads from ListingUI type only, no raw field access

GOOGLE SHEET
------------
Separate file: Apartment_Listings_Updated.xlsx
Replace your current Google Sheet data with this file.
All 66 column headers renamed to camelCase to match API output.
Your existing data rows are preserved — only Row 1 headers changed.

SUGGESTED COMMIT MESSAGE
------------------------
Build 3.1.13 — Field name cleanup: camelCase only, removed fallback aliases
