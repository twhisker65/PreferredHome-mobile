BUILD 3.1.09.3 HOTFIX
======================

Problem
- Add page crashed with: ReferenceError: Property 'loadProfileToggles' doesn't exist

Cause
- 'loadProfileToggles' was referenced from lib/profileStorage.ts, but the function/export was missing in the repo state.

Fix (Minimal)
- Restore lib/profileStorage.ts with loadProfileToggles/saveProfileToggles exports.
- Ensure Profile page owns the toggles UI and persists them.
- Ensure Add page ONLY reads toggles to gate sections (Schools/Parking/Pet Amenities); no toggles UI on Add.

Files included (changed only)
- lib/profileStorage.ts (restored)
- app/profile.tsx (profile toggles UI + persistence)
- app/(tabs)/add.tsx (reads toggles; no toggles UI)

Notes
- No other screens/components changed.
