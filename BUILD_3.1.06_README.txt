BUILD 3.1.06 (working build package)

Focus:
- Add Listing: include full listing data entry surface (open fields, dropdowns, multi-select, calendar picker)
- Listing Card: match required card contents; remove icon labels; move status under image; remove heart overlay; +fees logic
- Home: remove extra titles/instructions; Count -> Listings
- Listings: remove page title/header block; move Filter to top-right icon; cap compare at 4
- Calendar: remove title block + top divider; appointment days highlighted; appointment rows formatted as specified
- TopBar: hamburger +1.25x; optional right icon
- Tab bar: add thin top border line

Notes:
- Backend persistence for Add is still staged; Save is UI-only in this build.
