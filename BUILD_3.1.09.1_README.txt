BUILD 3.1.09.1 HOTFIX

Scope (only the items you flagged as missed in 3.1.09):
1) Fix base rent visibility by expanding Sheets header key mapping (base rent + fee fields).
2) Fix Compare toggle: centered icons + NO border (active icon is blue).
3) Fix Add date picker modal: fully centered + calendar not clipped.
4) Fix Calendar appointment line 1 formatting to use hyphens (Building - Date - Time).
5) Fix Profile toggles to be placeholder-only (no storage / no gating copy).

No other changes.
