PreferredHome — Build 3.1.07.1 Hotfix
Date: 2026-03-02

Fixes
- Fix JSX syntax error in app/(tabs)/add.tsx (stray </View> immediately after opening <ScrollView> caused bundling failure).

Scope
- Minimal change only to restore app build/run.

Apply
- Copy included files into repo root, preserving paths, overwrite existing.
- Restart Expo tunnel with cache clear: npx expo start --tunnel -c
